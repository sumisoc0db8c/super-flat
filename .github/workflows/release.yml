name: Release mcworld

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'

      - name: Build mcworld
        run: python main.py

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: '*.mcworld'
